# Can not convert a list of schemas
swagger: '2.0'
basePath: '/api'
info:
  title: example
  version: '1.0'
securityDefinitions:
  UserSecurity:
    type: apiKey
    in: header
paths:
  /:
    - $include: 'users/profile.yaml'
    - $include: 'users/login.yaml'
  /pets:
    $include: 'pets.yaml'
