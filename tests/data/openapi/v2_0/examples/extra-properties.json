{
  "swagger": "2.0",
  "info": {
    "version": "1.0.0",
    "title": "Extra properties example"
  },
  "host": "example.com",
  "basePath": "/api",
  "paths": {
    "/items": {
      "$name": "items",
      "get": {
        "description": "Returns all pets from the system that the user has access to",
        "$handler": "project.package.views.get_list",
        "$validate": true,
        "responses": {
          "200": {
            "description": "OK"
          }
        }
      }
    }
  }
}
