openapi: 3.1.0
info:
  title: API
  version: 1.0.0
paths:
  /pets/{pet_id}:
    get:
      parameters:
        - name: q
          in: query
          schema:
            type: string
        - name: h
          in: header
          schema:
            type: object
            properties:
              key:
                type: string
              value:
                type: string
          explode: true
        - name: pet_id
          in: path
          required: true
        - name: c
          in: cookie
          schema:
            type: string
          required: true
        - name: filter
          in: query
          content:
            application/json:
              schema:
                type: object
                properties:
                  type:
                    type: string
                  color:
                    type: string
      responses:
        '200':
          description: OK
